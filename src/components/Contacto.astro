---
import { MapLocation } from "@components/MapLocation.jsx";
---

<section class="max-w-7xl m-auto">
  <h2
    class="font-cinzel uppercase text-center py-5 text-2xl md:text-4xl font-normal bg-white tracking-widest"
  >
    Contacto
  </h2>

  <div
    class="flex flex-row justify-between items-center gap-3 bg-secondary p-5"
  >
    <div class="bg-button p-5 rounded">
      <form class="flex flex-col gap-5 flex-none" action="">
        <div class="flex flex-col justify-start items-center gap-2 box-content">
          <label
            class="font-cinzel tracking-wide text-lg font-semibold"
            for="name">Nombre y Apellido</label
          >
          <input class="w-full" type="text" name="name" />
        </div>
        <div class="flex flex-col justify-start items-center gap-2 box-content">
          <label
            class="font-cinzel tracking-wide text-lg font-semibold"
            for="message">Mensaje</label
          >
          <textarea class="w-full" name="message"></textarea>
        </div>
        <div>
          <button class="bg-secondary px-4 py-2 w-full rounded font-playfair"
            >Enviar</button
          >
        </div>
      </form>

      <div></div>
    </div>

    <div class="flex-1 flex flex-col justify-center items-center gap-2">
      <h5 class="font-cinzel text-pretty tracking-wider text-2xl">
        Cómo llegar
      </h5>
      <div class="flex w-full">
        <MapLocation client:only />
      </div>
    </div>
  </div>
</section>
